# Reviewed direct dependencies for CI/release maintenance.
# Workflows MUST install requirements/ci-lock.txt, which freezes the full dependency closure.
PyYAML==6.0.3
build==1.5.0
hatchling==1.32.0
pytest==9.1.1
pytest-cov==7.1.0
ruff==0.16.4
