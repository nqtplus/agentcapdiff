name: PR capability diff
on:
  pull_request:
permissions:
  contents: read
jobs:
  capability-diff:
    runs-on: ubuntu-24.04
    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7
        with:
          fetch-depth: 0
          persist-credentials: false
      - uses: actions/setup-python@5fda3b95a4ea91299a34e894583c3862153e4b97 # v7
        with:
          python-version: "3.12.14"
      - name: Materialize trusted base control plane
        shell: bash
        env:
          BASE_SHA: ${{ github.event.pull_request.base.sha }}
        run: |
          set -euo pipefail
          [[ "$BASE_SHA" =~ ^[0-9a-f]{40}$ ]]
          git worktree add --detach ../agentcapdiff-trusted-base "$BASE_SHA"
      - name: Verify trusted base runner provenance
        working-directory: ../agentcapdiff-trusted-base
        run: python scripts/check_ci_environment.py --python-version "3.12.14" --pip-version "26.2.1"
      - name: Install trusted base CI dependencies
        working-directory: ../agentcapdiff-trusted-base
        run: "python -m pip --isolated --disable-pip-version-check install --require-hashes --no-deps --no-cache-dir --only-binary=:all: --index-url=https://pypi.org/simple -r requirements/ci-lock.txt"
      - name: Verify trusted dependency closure
        working-directory: ../agentcapdiff-trusted-base
        run: python -m pip check
      - name: Install trusted base package
        working-directory: ../agentcapdiff-trusted-base
        run: python -m pip install . --no-deps --no-build-isolation
      - name: Snapshot base and head as untrusted static data
        shell: bash
        run: |
          set -euo pipefail
          BASE_DIR="$(cd ../agentcapdiff-trusted-base && pwd)"
          agentcapdiff snapshot "$BASE_DIR" \
            --policy "$BASE_DIR/agentcapdiff.yaml" \
            --output "$RUNNER_TEMP/base.json"
          agentcapdiff snapshot . \
            --policy agentcapdiff.yaml \
            --output "$RUNNER_TEMP/head.json"
      - name: Render capability diff
        shell: bash
        run: |
          set -euo pipefail
          agentcapdiff diff \
            "$RUNNER_TEMP/base.json" \
            "$RUNNER_TEMP/head.json" \
            --format markdown \
            --output "$RUNNER_TEMP/capability-diff.md"
          cat "$RUNNER_TEMP/capability-diff.md" >> "$GITHUB_STEP_SUMMARY"
