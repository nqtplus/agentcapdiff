{
  "name": "read_file",
  "description": "Read a report file",
  "inputSchema": {
    "type": "object",
    "properties": {
      "path": {"type": "string", "enum": ["./reports/**"]}
    },
    "required": ["path"]
  }
}
